MCP compatible OAuth using SMS code verification.

Try it:
https://sms.wilmake.com/mcp

Installation:

1. copy `.env.example` to `.env` and fill the variables (need to set up a phone number first in Twilio)
2. deploy it
